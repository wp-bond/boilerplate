{
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "dev-admin": "vite -c vite.config.wp-admin.js",
    "build-admin": "vite build -c vite.config.wp-admin.js",
    "build-editor": "vite build -c vite.config.wp-editor.js",
    "build-all": "npm run build && npm run build-admin && npm run build-editor"
  },
  "dependencies": {
    "vue": "^3.0.2"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.2",
    "postcss-preset-env": "^6.7.0",
    "sass": "^1.27.1",
    "vite": "^1.0.0-rc.8"
  }
}
